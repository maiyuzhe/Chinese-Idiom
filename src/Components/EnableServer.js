function EnableServer(){
  return (
    <div className='TranslateCard'>
      <h1>Server not found</h1>
      <p>Please clone the repository and enable the server with <code>npm run server</code></p>
      <a href="https://www.github.com/maiyuzhe">Repository Link</a>
    </div>
  )
}

export default EnableServer;